[package]
name = "coconut-registry"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
coconut-api = { path = "../api" }
coconut-integration-mpris = { path = "../../integrations/mpris", optional = true }
coconut-integration-upower = { path = "../../integrations/upower", optional = true }
coconut-integration-bluez = { path = "../../integrations/bluez", optional = true }
coconut-integration-brightnessctl = { path = "../../integrations/brightnessctl", optional = true }
coconut-integration-network-manager = { path = "../../integrations/network-manager", optional = true }
coconut-integration-power-profiles-daemon = { path = "../../integrations/power-profiles-daemon", optional = true }
coconut-integration-pipewire = { path = "../../integrations/pipewire", optional = true }
coconut-integration-kwin = { path = "../../integrations/kwin", optional = true }
coconut-integration-blair = { path = "../../integrations/blair", optional = true }
coconut-integration-windows = { path = "../../integrations/windows", optional = true }

[features]
default = [
    "mpris",
    "upower",
    "bluez",
    "brightnessctl",
    "network-manager",
    "power-profiles-daemon",
    "pipewire",
    "kwin",
    "blair",
]
mpris = ["dep:coconut-integration-mpris"]
upower = ["dep:coconut-integration-upower"]
bluez = ["dep:coconut-integration-bluez"]
brightnessctl = ["dep:coconut-integration-brightnessctl"]
network-manager = ["dep:coconut-integration-network-manager"]
power-profiles-daemon = ["dep:coconut-integration-power-profiles-daemon"]
pipewire = ["dep:coconut-integration-pipewire"]
kwin = ["dep:coconut-integration-kwin"]
blair = ["dep:coconut-integration-blair"]
windows = ["dep:coconut-integration-windows"]
